global:
  labels:
    ops.kodzik.cloud/chart: "platformex"
  defaults: { }

envRefs: { }

images: { }

instances:
  default:
    enableCrons: true
    enableWorkers: true
    crons: { }
    deployments: { }


