global:
  labels:
    ops.kodzik.cloud/chart: "platformex"
  defaults: { }

envRefs: { }

imageRefs: { }

instances:
  default:
    enableCrons: true
    enableWorkers: true
    crons: { }
    deployments: { }


